name: Test Workflow

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  test-job:
    name: Simple Test
    runs-on: ubuntu-latest
    
    steps:
    - name: Hello World
      run: echo "Hello World"
      
    - name: Check System
      run: |
        echo "Running on: ${{ runner.os }}"
        uname -a