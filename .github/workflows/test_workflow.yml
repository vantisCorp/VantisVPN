name: Test Workflow

on:
  push:
    branches: [fix/cicd-target-dependencies]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Echo
        run: echo "Test workflow running"